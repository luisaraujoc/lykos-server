############################################
# CONFIGURAÇÃO DO BANCO DE DADOS
############################################

# 🔹 PostgreSQL (principal)
spring.datasource.url=jdbc:postgresql://localhost:5432/lykos_db
spring.datasource.username=lykos
spring.datasource.password=lykos
spring.datasource.driver-class-name=org.postgresql.Driver

# 🔹 (Opcional) Se quiser trocar pra H2 rápido, só comentar o de cima e descomentar este
#spring.datasource.url=jdbc:h2:mem:lykosdb
#spring.datasource.driver-class-name=org.h2.Driver
#spring.datasource.username=sa
#spring.datasource.password=
#spring.h2.console.enabled=true
#spring.h2.console.path=/h2-console
#spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

############################################
# CONFIGURAÇÃO DO JPA / HIBERNATE
############################################

# Criação/atualização do schema:
# create-drop → recria a cada execução
# update → mantém dados e ajusta tabelas
# validate → só valida, não cria
spring.jpa.hibernate.ddl-auto=validate

# Mostrar SQL no console
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Dialeto padrão para PostgreSQL
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

############################################
# CONFIGURAÇÕES DA APLICAÇÃO
############################################

# Porta do servidor
server.port=8080

# Nome da aplicação
spring.application.name=lykos
